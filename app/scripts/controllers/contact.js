'use strict';

/**
 * @ngdoc function
 * @name weddingApp.controller:ContactCtrl
 * @description
 * # ContactCtrl
 * Controller of the weddingApp
 */
angular.module('weddingApp')
  .controller('ContactCtrl', ['$scope', '$window', function ($scope, $window) {
    
  }]);
